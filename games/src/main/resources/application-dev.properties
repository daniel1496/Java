# don't have Hibernate create and drop tables
# instead we want Spring to execute schema.sql and data.sql
spring.jpa.hibernate.ddl-auto=none

# have Hibernate output SQL to the logs
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true